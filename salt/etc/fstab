{%- import_yaml "config/fstab.yaml" as fstab with context -%}
{% for e in fstab.get('fstab', ()) -%}
{% if e.opts == "bind" -%}
{{ e.fs }}	{{ e.mp }}	none	{{ e.opts }}{{ e.get('extraopts', '') }}	0 0
{%- else -%}
{{ e.fs }}	{{ e.mp }}	{{ e.type }}	{{ e.opts }}{{ e.get('extraopts', '') }}	{{ e.dump }} {{ e.pass }}
{%- endif %}
{%- if e.get('bind') %}
{{ e.mp }}	/mnt/{{ grains['os'] | lower }}{{ e.mp }}	none	bind{{ e.get('extraopts', '') }}	0 0
{%- endif %}
{% endfor %}
